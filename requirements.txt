# Fruit Detection Model — Python Dependencies
# ============================================================
# IMPORTANT: Install PyTorch with CUDA BEFORE running pip install -r requirements.txt.
#
# For NVIDIA GPU (RTX 3060 / any CUDA 11.8–12.x):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# For CPU-only (slow, not recommended for training):
#   pip install torch torchvision
# ============================================================

# Core
numpy>=1.24.0
Pillow>=10.0.0
timm>=1.0.27
torchmetrics[detection]>=1.3.0

# Note: The custom detector does NOT use ultralytics or opencv-python, 
# resulting in a much leaner dependency footprint compared to the reference model.
