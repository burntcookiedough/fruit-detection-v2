# Quick start: install with pip
# For full dev dependencies, use:  pip install -e ".[dev]"
# See pyproject.toml for the canonical dependency specification.

torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
Pillow>=9.0.0
numpy>=1.24.0
